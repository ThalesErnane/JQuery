$(function(){
    $(".slider").slick( {
        dots: true, // mostra os pontos 
        infinite: true, // scroll
        speed: 300,
        slidesToShow: 1,
        adaptiveHeight: true
    });
});